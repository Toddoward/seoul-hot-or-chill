name: Seoul Hot/Chill Map Auto-Update

on:
  schedule:
    - cron: '*/10 * * * *'  # 10분마다 자동으로 실행
  workflow_dispatch:         # Actions 탭에서 수동으로도 실행 가능

jobs:
  build:
    runs-on: ubuntu-latest
    
    # 깃허브 액션이 레포지토리에 커밋을 할 수 있도록 권한 부여
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip' # 패키지 캐싱으로 속도 향상

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Map Generation Script
        env:
          SEOUL_API_KEY: ${{ secrets.SEOUL_API_KEY }}
          MAP_API_KEY: ${{ secrets.MAP_API_KEY }}
        run: python main.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 변경사항이 있을 때만 커밋 (이미지가 이전과 같으면 에러 방지)
          git add seoul_hot.png seoul_chill.png README.md
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🎨 Auto-update Seoul Map: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          fi